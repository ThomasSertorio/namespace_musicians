<h2>This is the edit for a musician</h2>
<%= simple_form_for([:musician, @musician]) do |f| %>
  <%= f.error_notification %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.button :submit %>
<% end %>
