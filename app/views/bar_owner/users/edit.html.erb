<h2>This is the edit for a bar_owner</h2>
<%= simple_form_for([:bar_owner, @bar_owner]) do |f| %>
  <%= f.error_notification %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.button :submit %>
<% end %>
